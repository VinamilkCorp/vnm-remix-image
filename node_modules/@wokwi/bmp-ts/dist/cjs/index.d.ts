import BmpDecoder from './decoder';
import BmpEncoder from './encoder';
import { IDecoderOptions, IImage } from './types';
export * from './types';
declare const _default: {
    decode: (bmpData: Uint8Array, options?: IDecoderOptions | undefined) => BmpDecoder;
    encode: (imgData: IImage) => BmpEncoder;
};
export default _default;
