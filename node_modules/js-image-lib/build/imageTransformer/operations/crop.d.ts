import { Color, ImageData } from "../../types/image";
export declare const cropImage: (src: ImageData, cropOptions: {
    x: number;
    y: number;
    width: number;
    height: number;
}, background: Color) => void;
